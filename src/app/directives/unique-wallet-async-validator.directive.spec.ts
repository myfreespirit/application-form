import { UniqueWalletAsyncValidatorDirective } from './unique-wallet-async-validator.directive';

describe('UniqueWalletAsyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueWalletAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
